Per partner:
Upload DON-hosted secret JSON for that partner (or versioned update), e.g.:
  { "AUTHORIZATION": "Bearer <partner_token>" }

Chainlink gives/uses slotId + version.

Configure router:
consumer = partner proxy address
enabled = true
slotId/version = partner’s values
Router then attaches those secrets to that partner’s request only.

brownie run scripts/configure_partner_auth.py main 0xRouter 0xPartnerProxy true 1 1 --network sepolia
Args:
1) router_address = Chainlink consumer contract
2) partner_proxy_address = partner caller identity for auth secrets mapping
3) enabled
4) slotId
5) version
If omitted, script can read from .env:
CORE_RATE_ROUTER_ADDRESS
PARTNER_PROXY_ADDRESS


make_enc_utils.py perform the full encryptSecrets(): 
-fetch DON/coordinator keys from chain,
-run EthCrypto-compatible public-key encryption, 
-run TDH2 threshold encryption, and 
-output final encryptedSecretsHexstring ready for upload_don_secrets.py.

.\scripts\make_enc_utils.py `
  --secrets-json .\test_sec.json `
  --private-key 0x... `
  --rpc-url https://sepolia.infura.io/v3/<KEY> `
  --functions-router-address 0xb83E47C2bC239B3bf370bc41e1459A34b41238D0 `
  --don-id fun-ethereum-sepolia-1 `


  .\venv\Scripts\python .\scripts\upload_don_secrets.py `
  --private-key 0x... `
  --don-id fun-ethereum-sepolia-1 `
  --gateway-urls "https://<gw1>,https://<gw2>" `
  --slot-id 1 `
  --minutes-until-expiration 60 `
  --encrypted-secrets-hex 0x...

  .\scripts\upload_don_secrets.py --slot-id 1